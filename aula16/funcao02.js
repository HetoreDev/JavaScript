function soma(n1=0, n2=0){
    return (n1 + n2)
}

console.log(`A soma dos valores dá ${soma(5,8)}`)

// Se na declaração das variáveis de parâmtro da função eu inicializar, mesmo que na chamada da função eu não passe os dois valores ele desconsidera soma ao valor da inicialização